version: '3.9'

services:
  redis:
    image: redis
    container_name: shop_broker
    volumes:
      - /redis_dump/:/var/lib/redis/
      - /redis_data/:/data/
    ports:
      - '127.0.0.1:63792:6379'
